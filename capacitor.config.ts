
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.helpaqui.app',
  appName: 'helpaquiapp',
  webDir: 'dist',
  plugins: {
    // Configure additional plugins here
  }
};

export default config;
